package com.tallermecanico.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuración CORS se maneja dentro de SecurityConfig.
 * Esta clase se mantiene vacía para evitar conflictos con el SecurityFilterChain.
 */
@Configuration
public class CorsConfig {
}
